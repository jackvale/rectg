{
    "name": "rectg-nav",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "node build-data.js && vite",
        "build": "node build-data.js && vite build",
        "preview": "vite preview"
    },
    "devDependencies": {
        "vite": "^5.0.0"
    },
    "dependencies": {
        "@vercel/analytics": "^1.6.1",
        "pinyin-match": "^1.2.10"
    }
}
