{
    "version": 2,
    "buildCommand": "cd site && npm install && npm run build",
    "outputDirectory": "site/dist",
    "framework": "astro",
    "ignoreCommand": "git rev-parse --verify HEAD^ >/dev/null 2>&1 && git diff --quiet HEAD^ HEAD -- site || exit 1"
}
